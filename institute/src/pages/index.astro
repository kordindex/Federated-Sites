---
import BrandSwitcher from '../../../shared/BrandSwitcher.astro';
---
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Institute for UNLearning | Workplace Culture IP</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&family=Playfair+Display:ital,wght@0,700;0,900;1,700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-mist: #EAEAEA;
            --ink-institute: #111e1c; 
            --gold-accent: #b59353;    
            --silver-gray: #b1b3b3;    
            --transition: 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            --nav-height-t1: 90px;
            --nav-height-t2: 70px;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body { 
            font-family: 'Inter', sans-serif; 
            background: var(--bg-mist); 
            color: var(--ink-institute); 
            overflow-x: hidden; 
        }
        
        .serif { font-family: 'Playfair Display', serif; }
        .sans { font-family: 'Inter', sans-serif; text-transform: uppercase; letter-spacing: 1px; }

        /* --- TIER 2: INSTITUTE NAV --- */
        .site-header {
            position: fixed; 
            top: var(--nav-height-t1); 
            width: 100%; 
            z-index: 1000;
            background: rgba(255, 255, 255, 0.95); 
            backdrop-filter: blur(10px);
            border-bottom: 1px solid var(--silver-gray); 
            height: var(--nav-height-t2);
            display: flex; 
            align-items: center; 
            transition: var(--transition);
        }
        .site-nav-links { display: flex; gap: 2rem; list-style: none; font-size: 0.7rem; font-weight: 700; }
        .site-nav-links a { text-decoration: none; color: inherit; transition: 0.3s; }
        .site-nav-links a:hover { color: var(--gold-accent); }

        /* --- CONTENT --- */
        .hero { padding: calc(var(--nav-height-t1) + var(--nav-height-t2) + 60px) 0 80px; min-height: 80vh; }
        .container { max-width: 1250px; margin: 0 auto; padding: 0 2rem; }
        
        h1 { 
            font-size: clamp(2.5rem, 7vw, 5.5rem); 
            line-height: 1.1; 
            margin-bottom: 2rem; 
            font-family: 'Playfair Display', serif;
            font-weight: 900;
        }
        p.lead { 
            font-size: 1.5rem; 
            max-width: 850px; 
            margin-bottom: 3rem; 
            font-family: 'Playfair Display', serif;
        }

        .grid-3 { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem; padding: 60px 0; }
        .card { padding: 3rem; border: 1px solid var(--silver-gray); background: #fff; }
        .card h3 { font-size: 1.4rem; margin-bottom: 1rem; color: var(--gold-accent); font-family: 'Playfair Display', serif; font-weight: 700; }

        /* --- RESPONSIVE REFACTOR --- */
        @media (max-width: 768px) {
            :root {
                --nav-height-t1: 65px;
                --nav-height-t2: 55px;
            }
            
            .site-header {
                overflow-x: auto;
                -webkit-overflow-scrolling: touch;
                justify-content: flex-start;
                padding: 0 1rem;
            }
            .site-header .container { justify-content: flex-start !important; gap: 2rem; }
            .site-nav-links { gap: 1.5rem; padding-right: 2rem; }
            .site-nav-links li { white-space: nowrap; }
            .site-header span.serif { display: none; } 
            
            .hero { padding-top: 140px; text-align: center; }
            h1 { font-size: 2.8rem; }
            p.lead { font-size: 1.25rem; }
            .card { padding: 2rem; }
        }

        footer { background: #000; color: #fff; padding: 80px 0; text-align: center; }
    </style>
</head>
<body>

    <BrandSwitcher activeBrand="institute" />

    <header class="site-header">
        <div class="container" style="display:flex; justify-content: space-between; width: 100%;">
            <span class="serif" style="font-weight:900; font-size: 1.2rem;">Institute <span style="color:var(--gold-accent)">UNLearning</span></span>
            <ul class="site-nav-links sans">
                <li><a href="#">Methodology</a></li>
                <li><a href="#">Research</a></li>
                <li><a href="#">Media</a></li>
            </ul>
        </div>
    </header>

    <main class="hero">
        <div class="container">
            <span class="sans" style="color:var(--gold-accent); font-size:0.8rem; font-weight:800; display:block; margin-bottom:1.5rem;">IP Anchor & Thought Leadership</span>
            <h1 class="serif">Workplace Culture That <br><span style="font-style:italic;">Grows Itself.</span></h1>
            <p class="lead serif">Serving as the credibility anchor and IP holder for the world's most critical organizations.</p>
            
            <div class="grid-3">
                <div class="card">
                    <h3>Thought Leadership Hub</h3>
                    <p>State of Workplace Culture Reports and Academic White Papers.</p>
                </div>
                <div class="card">
                    <h3>IP Library</h3>
                    <p>Foundational Frameworks and Certification Programs.</p>
                </div>
            </div>
        </div>
    </main>

    <footer>
        <div class="container">
            <p style="opacity: 0.5; font-size: 0.7rem; text-transform: uppercase; letter-spacing: 2px;">An Institute for Unlearning Brand</p>
        </div>
    </footer>

</body>
</html>