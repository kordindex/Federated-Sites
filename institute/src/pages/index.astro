---
// Institute for UNLearning - Master Brand Island (Complete Refactor)
---
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Institute for UNLearning | Workplace Culture IP</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&family=Playfair+Display:ital,wght@0,700;0,900;1,700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-mist: #EAEAEA;
            --ink-institute: #111e1c; /* PMS 627/5467 Deep Green [cite: 38-41] */
            --gold-accent: #b59353;    /* PMS 1245/131 [cite: 52-55] */
            --silver-gray: #b1b3b3;    /* PMS 443 [cite: 48-49] */
            --transition: 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            --nav-height-t1: 90px;
            --nav-height-t2: 70px;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body { 
            font-family: 'Inter', sans-serif; 
            background: var(--bg-mist); 
            color: var(--ink-institute); 
            overflow-x: hidden; 
        }
        
        .serif { font-family: 'Playfair Display', serif; }
        .sans { font-family: 'Inter', sans-serif; text-transform: uppercase; letter-spacing: 1px; }

        /* --- TIER 1: BRAND SWITCHER --- */
        .fed-nav {
            position: fixed; top: 0; width: 100%; z-index: 2000;
            display: grid; grid-template-columns: 1fr 1fr 1fr;
            height: var(--nav-height-t1); background: #fff; border-bottom: 4px solid var(--ink-institute);
            transition: var(--transition);
        }
        .pillar {
            display: flex; flex-direction: column; justify-content: center; align-items: center;
            text-decoration: none; transition: var(--transition);
            border-bottom: 6px solid transparent; text-align: center;
            padding: 0 10px;
        }
        .pillar span { font-size: 0.55rem; text-transform: uppercase; font-weight: 800; opacity: 0.6; }
        .pillar h4 { font-size: 0.8rem; margin-top: 4px; font-family: 'Inter', sans-serif; text-transform: uppercase;}
        
        .pillar-inst { background: var(--ink-institute); color: #fff; opacity: 1; border-bottom: 8px solid #fff; }
        .pillar-cohe { background: #1b263b; color: #fff; opacity: 0.4; }
        .pillar-coac { background: var(--gold-accent); color: #fff; opacity: 0.4; }

        /* --- TIER 2: INSTITUTE NAV --- */
        .site-header {
            position: fixed; top: var(--nav-height-t1); width: 100%; z-index: 1000;
            background: rgba(255, 255, 255, 0.95); backdrop-filter: blur(10px);
            border-bottom: 1px solid var(--silver-gray); height: var(--nav-height-t2);
            display: flex; align-items: center; transition: var(--transition);
        }
        .site-nav-links { display: flex; gap: 2rem; list-style: none; font-size: 0.7rem; font-weight: 700; }
        .site-nav-links a { text-decoration: none; color: inherit; transition: 0.3s; }
        .site-nav-links a:hover { color: var(--gold-accent); }

        /* --- HERO: ACADEMIC & CONTRAST --- */
        .hero { padding: calc(var(--nav-height-t1) + var(--nav-height-t2) + 60px) 0 80px; min-height: 80vh; }
        .container { max-width: 1250px; margin: 0 auto; padding: 0 2rem; }
        
        /* Main Titles: Mixed Punctuation, NO Cap Lock  */
        h1 { 
            font-size: clamp(2.5rem, 7vw, 5.5rem); 
            line-height: 1.1; 
            margin-bottom: 2rem; 
            font-family: 'Playfair Display', serif;
            font-weight: 900;
        }
        p.lead { 
            font-size: 1.5rem; 
            max-width: 850px; 
            margin-bottom: 3rem; 
            font-family: 'Playfair Display', serif;
        }

        .grid-3 { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem; padding: 60px 0; }
        .card { padding: 3rem; border: 1px solid var(--silver-gray); background: #fff; }
        .card h3 { font-size: 1.4rem; margin-bottom: 1rem; color: var(--gold-accent); font-family: 'Playfair Display', serif; font-weight: 700; }

        /* --- RESPONSIVE REFACTOR --- */
        @media (max-width: 768px) {
            :root {
                --nav-height-t1: 65px;
                --nav-height-t2: 55px;
            }
            .pillar h4 { font-size: 0.65rem; }
            .pillar span { display: none; }
            
            /* Horizontal Scroll Mobile Nav */
            .site-header {
                overflow-x: auto;
                -webkit-overflow-scrolling: touch;
                justify-content: flex-start;
                padding: 0 1rem;
            }
            .site-header .container { justify-content: flex-start !important; gap: 2rem; }
            .site-nav-links { gap: 1.5rem; padding-right: 2rem; }
            .site-nav-links li { white-space: nowrap; }
            .site-header span.serif { display: none; } 
            
            .hero { padding-top: 140px; text-align: center; }
            h1 { font-size: 2.8rem; } /* [cite: 3] */
            p.lead { font-size: 1.25rem; }
            .card { padding: 2rem; }
        }

        footer { background: #000; color: #fff; padding: 80px 0; text-align: center; }
    </style>
</head>
<body>

    <nav class="fed-nav">
        <a href="#" class="pillar pillar-inst">
            <span>The Master Brand</span>
            <h4>The Institute</h4>
        </a>
        <a href="https://federated-sites-hmy5.vercel.app/" class="pillar pillar-cohe">
            <span>B2B / Organizations</span>
            <h4>The Coherence Co.</h4>
        </a>
        <a href="https://federated-sites.vercel.app/" class="pillar pillar-coac">
            <span>B2C / Individuals</span>
            <h4>Kahane Coaching</h4>
        </a>
    </nav>

    <header class="site-header">
        <div class="container" style="display:flex; justify-content: space-between; width: 100%;">
            <span class="serif" style="font-weight:900; font-size: 1.2rem;">Institute <span style="color:var(--gold-accent)">UNLearning</span></span>
            <ul class="site-nav-links sans">
                <li><a href="#">Methodology</a></li>
                <li><a href="#">Research</a></li>
                <li><a href="#">Media</a></li>
            </ul>
        </div>
    </header>

    <main class="hero">
        <div class="container">
            <span class="sans" style="color:var(--gold-accent); font-size:0.8rem; font-weight:800; display:block; margin-bottom:1.5rem;">IP Anchor & Thought Leadership</span>
            <h1 class="serif">Workplace Culture That <br><span style="font-style:italic;">Grows Itself.</span></h1>
            <p class="lead serif">Serving as the credibility anchor and IP holder for the world's most critical organizations.</p>
            
            <div class="grid-3">
                <div class="card">
                    <h3>Thought Leadership Hub</h3>
                    <p>State of Workplace Culture Reports and Academic White Papers[cite: 1, 8].</p>
                </div>
                <div class="card">
                    <h3>IP Library</h3>
                    <p>Foundational Frameworks and Certification Programs[cite: 1, 8].</p>
                </div>
            </div>
        </div>
    </main>

    <footer>
        <div class="container">
            <p style="opacity: 0.5; font-size: 0.7rem; text-transform: uppercase; letter-spacing: 2px;">An Institute for Unlearning Brand</p>
        </div>
    </footer>

</body>
</html>